{
  "name": "ddash",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build && bun scripts/inline.ts",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "6"
  },
  "dependencies": {
    "lz-string": "^1.5.0",
    "mermaid": "^11"
  }
}
